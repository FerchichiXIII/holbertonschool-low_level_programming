#include "main.h"
/**
 *_strlen - returns the length of a string.
 *
 *@s: strlen to n
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */

int _strlen(char *s)

{

	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		i = i;
	}

	return (i);
}
