#include "main.h"
/**
 *_puts - returns the length of a string.
 *
 *@str: prints a string
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */

void _puts(char *str)
{
	puts(str);
}
